# Copy to .env and set real values. Server-only (not in Vite define).

# Google OAuth — create at https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxx

# Session encryption — generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=<32-byte random hex>

# App URL — use https://your-domain.com in production (for OAuth callback)
APP_URL=http://localhost:3000

# HTTPS (Nginx + Certbot) — set for production to get Let's Encrypt certs
# DOMAIN=your-domain.com
# LETSENCRYPT_EMAIL=you@example.com

# If port 80 (or 443) is already in use on the server, use different host ports for this app's nginx:
# NGINX_HTTP_PORT=8080
# NGINX_HTTPS_PORT=8443

# PostgreSQL (used for all app data + sessions + TTS cache). In Docker Compose this is set automatically.
# DATABASE_URL=postgresql://flashcards:flashcards@localhost:5432/flashcards

# Gemini API (for TTS/quiz). Can be exposed to client for live features.
GEMINI_API_KEY=
